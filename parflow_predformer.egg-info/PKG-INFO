Metadata-Version: 2.4
Name: parflow-predformer
Version: 0.2.0
Summary: PredFormer surrogate modeling for ParFlow pressure prediction
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: einops
Requires-Dist: fvcore
Requires-Dist: numpy==1.26.4
Requires-Dist: opencv-python
Requires-Dist: pftools
Requires-Dist: timm<=0.6.11,>=0.5.4
Requires-Dist: torch>=2.0
Requires-Dist: torchvision
Requires-Dist: tqdm
Dynamic: description
Dynamic: description-content-type
Dynamic: requires-dist
Dynamic: requires-python
Dynamic: summary

# ParFlow-PredFormer

使用 PredFormer 根据连续 ParFlow 状态预测未来压力场。当前默认输入为前 12 小时数据，预测后 12 小时 pressure。

## 数据组成

单个输入时刻包含 45 个通道：

- pressure：10 通道
- evaptrans：4 通道
- static：31 通道

模型输出未来 12 个时刻的 10 个 pressure 通道。默认空间窗口为 `64 × 80`，步长为 `32 × 40`。

数据目录：

```text
data/parflow/
├── normal_data/
│   ├── press/<year>/*.pfb
│   ├── evaptrans/<year>/*.pfb
│   ├── saturation/<year>/*.pfb     （optional，当前默认未使用）
│   └── static/static.pfb
├── extra_data_apcp14/
│   ├── press/<year>/*.pfb
│   ├── evaptrans/<year>/*.pfb
│   └── saturation/<year>/*.pfb     （optional，当前默认未使用）
└── extra_data_index/
    └── *.csv
```

pressure 时刻 `h` 与 evaptrans 时刻 `h+1` 对齐。

## 项目结构

```text
configs/parflow/       实验和模型配置
openstl/api/           训练流程
openstl/methods/       PredFormer 训练方法
openstl/models/        模型实现
openstl/datasets/      ParFlow 数据读取、切分和采样
model_deployment/      推理入口
tools/                 manifest 和统计量生成工具
scripts/parflow/       训练脚本
data/parflow/extra_data_index/  本地极端事件索引
extra_data/            可提交的极端事件 manifest 备份
```

## 安装

```bash
conda env create -f environment.yml
conda activate predformer
pip install -e .
```

## 训练

训练任务应通过 SLURM 提交到 GPU 节点。打开 `scripts/parflow/parflow_PredFormer_FacTS_train.sh` 顶部的“用户配置区”，直接指定仓库路径、Python、GPU、数据路径、年份划分、extra data 和训练超参数。

常用入口：

```bash
sbatch /home/huanghui/data/ParFlow-transformer/slurm/parflow_train.sh
```

训练脚本中现在保留显式配置，例如：

```bash
REPO=/home/huanghui/data/ParFlow-transformer
PYTHON_BIN=/home/huanghui/data/anaconda3/envs/predformer/bin/python
export CUDA_VISIBLE_DEVICES=1
DATA_ROOT=/home/huanghui/data/ParFlow-transformer/data/parflow/normal_data
USE_EXTRA_DATA=True
```

配置职责：

```text
configs/parflow/PredFormer.py                    模型结构和输入/输出通道
scripts/parflow/parflow_PredFormer_FacTS_train.sh  本次训练的数据、GPU、年份和训练超参数
openstl/utils/default_config.py                  框架级默认值
```

## 生成极端事件 manifest

```bash
python tools/build_extra_manifest_from_candidates.py \
  --apcp-dir /path/to/APCP/2020 \
  --apcp-dir /path/to/APCP/2021 \
  --extra-data-root data/parflow/extra_data_apcp14 \
  --mode regime \
  --regimes moderate,heavy \
  --output data/parflow/extra_data_index/moderate_heavy.csv
```

## 计算标准化统计量

```bash
python tools/compute_mean_std_with_extra_manifest.py \
  --normal-data-root data/parflow/normal_data \
  --normal-years 2020,2021 \
  --extra-data-root data/parflow/extra_data_apcp14 \
  --extra-manifest data/parflow/extra_data_index/moderate_heavy.csv \
  --output stats/moderate_heavy.npz
```

加入额外数据时，必须使用与普通数据和当前 manifest 共同计算的统计量。

## 推理

```bash
sbatch /home/huanghui/data/ParFlow-transformer/slurm/parflow_inference.sh
```

推理参数在 `scripts/parflow/parflow_PredFormer_FacTS_inference.sh` 顶部的“用户配置区”修改。也可以直接运行：

```bash
python -m model_deployment.inference \
  --run-dir work_dirs/ParFlow_press/<experiment> \
  --start-hour 20190000 \
  --end-hour 20198759 \
  --output-dir inference_data/press \
  --run-name test_2019
```

## Checkpoint

- `--resume-from`：恢复模型、优化器、scheduler 和 epoch。
- `--finetune-from`：只加载模型权重，从新的优化过程开始。

训练输出位于：

```text
work_dirs/<experiment>/
├── model_param.json
├── checkpoints/latest.pth
└── checkpoints/epoch_XXXX.pth
```

## GitHub 数据约定

`data/`、`work_dirs/`、`stats/`、`inference_data/` 和结果分析目录默认不会提交。仓库只保存代码、配置和可复现的 manifest。
